# version: '3.8'
# services:
#   mysql-db:
#     image: mysql:8.0
#     environment:
#       MYSQL_ROOT_PASSWORD: 1234
#       MYSQL_DATABASE: vehiclerent
#       MYSQL_USER: root
#       MYSQL_PASSWORD: 1234
#     ports:
#       - "3306:3306" # Optional: Expose MySQL port to host
#     volumes:
#       - mysql-data:/var/lib/mysql # Persist MySQL data

#   spring-boot-app:
#     build: .
#     ports:
#       - "8080:8080"
#     depends_on:
#       - mysql-db
#     environment:
#       SPRING_DATASOURCE_URL: jdbc:mysql://mysql-db:3306/your_database_name?useSSL=false&allowPublicKeyRetrieval=true
#       SPRING_DATASOURCE_USERNAME: root
#       SPRING_DATASOURCE_PASSWORD: 1234

# volumes:
#   mysql-data:
version: '3.8'

services:
  # MySQL Database
  mysql-db:
    image: mysql:8.0
    container_name: mysql-db
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: 1234
      MYSQL_DATABASE: vehicle_rental1
    ports:
      - "3309:3306"  # Map host port 3309 to container port 3306
    volumes:
      - mysql-data:/var/lib/mysql
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
    networks:
      - app-network
    command: --default-authentication-plugin=mysql_native_password

  # Spring Boot Backend
  backend:
    build: ./vehiclerentnew
    container_name: spring-backend
    restart: unless-stopped
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://mysql-db:3306/vehicle_rental1
      SPRING_DATASOURCE_USERNAME: root
      SPRING_DATASOURCE_PASSWORD: 1234
      SPRING_JPA_HIBERNATE_DDL_AUTO: update
    ports:
      - "8080:8080"
    depends_on:
      - mysql-db
    networks:
      - app-network

  # React Frontend
  frontend:
    build: ./my-app
    container_name: react-frontend
    restart: unless-stopped
    ports:
      - "80:80"
    depends_on:
      - backend
    networks:
      - app-network

volumes:
  mysql-data:

networks:
  app-network:
    driver: bridge